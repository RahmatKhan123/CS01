//input validation by user. write a program to ask the user input and repeat asking until the right input is inserted.
#include <iostream>
using namespace std;
int main()
{
  
  /*int num1;
  cout << "Input number in the range 0 to 100 " << endl;
  cin >> num1;

  while ((num1 <=0) || (num1 >= 100))
  {
    cout << "INput Error! please input again:" << endl;
    cin >> num1;
    if ((num1 >= 0) && (num1 <= 100))
    cout << " good job that is correct Input: " << num1 << endl; 
  }
  /*int num1;
  cout << "Enter a number in the range 0 to 100: " << endl;
  cin >> num1;

  while ((num1 <= 0) || (num1 >= 100))
  {
    cout << "incorrect Input:" << endl;
    cout << "Input again: " << endl;
    cin >> num1;

    if ((num1 >= 0) && (num1 <= 100))
    cout << "Goodee Job: that is the correct input:" << num1 << endl;
  }
  const int min_number = 1, max_number =10;
  int num = min_number;
  cout << "Number.    Number squared:" << endl;
  cout << "---------------\n";

  while (num <= max_number)
  {
    cout << num << "\t\t" << (num * num ) << endl;
    num++;
  }
  const int min=1, max = 100;
  int num = min;
  cout << "numbwe        number sqared" << endl;
  cout << "______________" << endl;
  while(num <= max)
  {
    cout << num << "\t\t" << (num * num) << endl;
    num++;
  }*/
  int v = 10;
  do
  {
    cout << v << endl;
    while(v<10)
  }


}
